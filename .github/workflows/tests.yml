name: Run Tests and E2E

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'
      - 'README.*.md'
      - 'LICENSE'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install -r requirements-dev.txt

      - name: Run Unit Tests
        run: |
          pytest --cov=./ --cov-report=xml tests/ -v

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: artryazanov/gemini-readme-translator

      - name: Create Test Input README
        run: |
          echo "# Gemini Test Action" > TEST_README.md
          echo "This is a quick test to verify the E2E pipeline for translation." >> TEST_README.md

      - name: Run E2E Test (Gemini README Translator)
        id: translator
        uses: ./
        with:
          api_key: ${{ secrets.GEMINI_API_KEY }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          source_file: "TEST_README.md"
          languages: 'ru, es'
          add_language_menu: 'true'
          skip_commit: 'true'
          model: 'gemini-3.1-pro-preview'

      - name: Verify Results
        run: |
          echo "Checking generated files..."
          
          if [ ! -f "TEST_README.ru.md" ]; then
            echo "::error::TEST_README.ru.md not found!"
            exit 1
          fi
          
          if [ ! -f "TEST_README.es.md" ]; then
            echo "::error::TEST_README.es.md not found!"
            exit 1
          fi
          
          echo "Files exist. Checking content..."
          cat TEST_README.ru.md
          
          printf "\nChecking GitHub output variables...\n"
          echo "Duration: ${{ steps.translator.outputs.duration_seconds }} s"
          echo "Total Tokens: ${{ steps.translator.outputs.total_tokens_used }}"
          
          if [ -z "${{ steps.translator.outputs.total_tokens_used }}" ]; then
            echo "::error::total_tokens_used is empty!"
            exit 1
          fi
          if [ "${{ steps.translator.outputs.total_tokens_used }}" = "0" ]; then
            echo "::error::total_tokens_used is 0, which suggests API call failed or stats weren't parsed."
            exit 1
          fi
